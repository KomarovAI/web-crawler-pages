name: 🌐 Deploy to Pages

on:
  workflow_dispatch:
    inputs:
      source_repo:
        description: 'Source repository (KomarovAI/web-crawler)'
        required: true
        default: 'KomarovAI/web-crawler'
      run_id:
        description: 'Workflow run ID from web-crawler (look at Actions tab)'
        required: true
      site_domain:
        description: 'Domain folder name (e.g., callmedley-com)'
        required: true
        default: 'callmedley-com'

jobs:
  # ============================================================
  # JOB 1: VERIFY - Check database integrity
  # ============================================================
  verify:
    name: '🔍 Verify Database'
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      actions: read
    
    steps:
      - name: 📥 Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: false
          repository: ${{ inputs.source_repo }}
          run-id: ${{ inputs.run_id }}
          github-token: ${{ secrets.GH_PAT }}

      - name: 🗜️ Extract ZIP archives (recursive)
        run: |
          find artifacts -name "*.zip" -type f | while read zip; do
            dir=$(dirname "$zip")
            echo "Extracting: $zip to $dir"
            unzip -q "$zip" -d "$dir"
            rm "$zip"
          done
          echo "---"
          echo "Final structure:"
          find artifacts -type f

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: ✅ Check database files
        run: |
          python3 << 'EOF'
          import sqlite3
          from pathlib import Path
          import sys
          import os
          
          artifacts_dir = Path('artifacts')
          
          print(f"\n🔍 Searching in: {artifacts_dir}")
          
          # Check if artifacts directory exists
          if not artifacts_dir.exists():
            print(f"❌ Artifacts directory does not exist!")
            print(f"📖 Current working directory: {os.getcwd()}")
            print(f"📂 Contents of current dir: {os.listdir('.')}")
            print("\n⚠️  Make sure:")
            print("  1. Run ID is CORRECT (from web-crawler Actions tab)")
            print("  2. That run has completed successfully")
            print("  3. Artifacts were uploaded in that run")
            sys.exit(1)
          
          print(f"📂 Directory contents: {list(artifacts_dir.iterdir())}")
          
          db_files = list(artifacts_dir.glob('db-*/*.db'))
          
          if not db_files:
            print("❌ No database files found!")
            print("⚠️  Looking for pattern: db-*/*.db")
            print("\n🔍 Searching recursively...")
            db_files = list(artifacts_dir.glob('**//*.db'))
            if db_files:
              print(f"Found {len(db_files)} DB files recursively:")
              for f in db_files:
                print(f"  - {f}")
            print("\n⚠️  Make sure:")
            print("  1. Run ID is CORRECT (from web-crawler Actions tab)")
            print("  2. That run has completed successfully")
            print("  3. Artifacts were uploaded")
            sys.exit(1)
          
          print(f"\n✅ Found {len(db_files)} database(s)\n")
          
          for db_file in sorted(db_files):
            domain = db_file.parent.name.replace('db-', '')
            size_mb = db_file.stat().st_size / 1024 / 1024
            
            print(f"📦 {domain} ({size_mb:.2f} MB)")
            
            # Check SQLite format
            with open(db_file, 'rb') as f:
              if f.read(16) != b'SQLite format 3':
                print(f"  ❌ Invalid SQLite format!")
                sys.exit(1)
            
            # Connect and verify
            try:
              conn = sqlite3.connect(str(db_file))
              cursor = conn.cursor()
              
              cursor.execute("SELECT COUNT(*) FROM pages")
              pages = cursor.fetchone()[0]
              print(f"  ✅ Pages: {pages}")
              
              cursor.execute("PRAGMA integrity_check")
              if cursor.fetchone()[0] != 'ok':
                print(f"  ❌ Database corrupted!")
                sys.exit(1)
              
              print(f"  ✅ Integrity: OK")
              conn.close()
              
            except Exception as e:
              print(f"  ❌ Error: {e}")
              sys.exit(1)
          
          print("\n✅ All databases verified!\n")
          EOF

  # ============================================================
  # JOB 2: BUILD - Convert database to HTML
  # ============================================================
  build:
    name: '🏗️ Build HTML'
    needs: verify
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      actions: read
    
    steps:
      - name: 📥 Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: false
          repository: ${{ inputs.source_repo }}
          run-id: ${{ inputs.run_id }}
          github-token: ${{ secrets.GH_PAT }}

      - name: 🗜️ Extract ZIP archives (recursive)
        run: |
          find artifacts -name "*.zip" -type f | while read zip; do
            dir=$(dirname "$zip")
            echo "Extracting: $zip to $dir"
            unzip -q "$zip" -d "$dir"
            rm "$zip"
          done
          echo "---"
          echo "Final structure:"
          find artifacts -type f

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 🏗️ Build HTML pages
        run: |
          python3 << 'EOF'
          import sqlite3
          import shutil
          from pathlib import Path
          from datetime import datetime
          
          artifacts_dir = Path('artifacts')
          output_dir = Path('sites')
          output_dir.mkdir(exist_ok=True)
          
          print("\n" + "="*60)
          print("🏗️  BUILDING STATIC SITE")
          print("="*60 + "\n")
          
          for db_dir in sorted(artifacts_dir.glob('db-*')):
            domain_folder = db_dir.name.replace('db-', '')
            db_file = db_dir / f"{domain_folder}.db"
            
            if not db_file.exists():
              continue
            
            print(f"📦 Building: {domain_folder}")
            
            site_dir = output_dir / domain_folder
            site_dir.mkdir(exist_ok=True)
            pages_dir = site_dir / 'pages'
            pages_dir.mkdir(exist_ok=True)
            
            shutil.copy(db_file, site_dir / f"{domain_folder}.db")
            
            conn = sqlite3.connect(str(db_file))
            conn.row_factory = sqlite3.Row
            cursor = conn.cursor()
            
            cursor.execute("SELECT id, url, title, content, status_code FROM pages ORDER BY id")
            pages = cursor.fetchall()
            
            print(f"  📄 {len(pages)} pages")
            
            # Create index page
            html_content = '<!DOCTYPE html>\n'
            html_content += '<html lang="en">\n'
            html_content += '<head>\n'
            html_content += '<meta charset="UTF-8">\n'
            html_content += '<meta name="viewport" content="width=device-width, initial-scale=1.0">\n'
            html_content += f'<title>{domain_folder}</title>\n'
            html_content += '<style>\n'
            html_content += '* { margin: 0; padding: 0; box-sizing: border-box; }\n'
            html_content += 'body { \n'
            html_content += '  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;\n'
            html_content += '  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);\n'
            html_content += '  min-height: 100vh;\n'
            html_content += '  padding: 40px 20px;\n'
            html_content += '}\n'
            html_content += '.container { max-width: 1200px; margin: 0 auto; }\n'
            html_content += 'h1 { color: white; text-align: center; margin-bottom: 40px; font-size: 2.5em; }\n'
            html_content += '.grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 20px; }\n'
            html_content += '.card {\n'
            html_content += '  background: white;\n'
            html_content += '  border-radius: 8px;\n'
            html_content += '  padding: 20px;\n'
            html_content += '  box-shadow: 0 4px 15px rgba(0,0,0,0.2);\n'
            html_content += '  transition: transform 0.2s;\n'
            html_content += '}\n'
            html_content += '.card:hover { transform: translateY(-5px); }\n'
            html_content += '.card h3 { color: #333; margin-bottom: 10px; }\n'
            html_content += '.card p { color: #666; font-size: 0.9em; word-break: break-all; }\n'
            html_content += '.card a {\n'
            html_content += '  display: inline-block;\n'
            html_content += '  margin-top: 15px;\n'
            html_content += '  padding: 8px 12px;\n'
            html_content += '  background: #667eea;\n'
            html_content += '  color: white;\n'
            html_content += '  text-decoration: none;\n'
            html_content += '  border-radius: 4px;\n'
            html_content += '}\n'
            html_content += '.card a:hover { background: #764ba2; }\n'
            html_content += '</style>\n'
            html_content += '</head>\n'
            html_content += '<body>\n'
            html_content += '<div class="container">\n'
            html_content += f'<h1>📖 {domain_folder}</h1>\n'
            html_content += '<div class="grid">\n'
            
            for idx, page in enumerate(pages, 1):
              page_num = f"{idx:05d}"
              title = (page['title'] or 'Untitled')[:40]
              url = page['url'][:60]
              status = page['status_code']
              
              html_content += '<div class="card">\n'
              html_content += f'<h3>{title}</h3>\n'
              html_content += f'<p>{url}</p>\n'
              html_content += f'<small>Status: {status}</small>\n'
              html_content += f'<a href="pages/page-{page_num}.html">View →</a>\n'
              html_content += '</div>\n'
            
            html_content += '</div>\n'
            html_content += '</div>\n'
            html_content += '</body>\n'
            html_content += '</html>\n'
            
            with open(site_dir / 'index.html', 'w', encoding='utf-8') as f:
              f.write(html_content)
            
            # Create individual page files
            for idx, page in enumerate(pages, 1):
              page_num = f"{idx:05d}"
              page_file = pages_dir / f"page-{page_num}.html"
              
              page_html = '<!DOCTYPE html>\n'
              page_html += '<html lang="en">\n'
              page_html += '<head>\n'
              page_html += '<meta charset="UTF-8">\n'
              page_html += '<meta name="viewport" content="width=device-width, initial-scale=1.0">\n'
              page_html += f'<title>{(page["title"] or "Page")[:50]}</title>\n'
              page_html += '<style>\n'
              page_html += '* { margin: 0; padding: 0; box-sizing: border-box; }\n'
              page_html += 'body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; background: #f5f5f5; }\n'
              page_html += '.container { max-width: 900px; margin: 0 auto; background: white; padding: 30px; }\n'
              page_html += '.header { border-bottom: 2px solid #667eea; margin-bottom: 30px; }\n'
              page_html += '.header h1 { color: #667eea; }\n'
              page_html += '.meta { color: #666; font-size: 0.9em; margin: 5px 0; }\n'
              page_html += '.nav a { display: inline-block; padding: 8px 15px; background: #667eea; color: white; text-decoration: none; border-radius: 4px; }\n'
              page_html += '.nav a:hover { background: #764ba2; }\n'
              page_html += '</style>\n'
              page_html += '</head>\n'
              page_html += '<body>\n'
              page_html += '<div class="container">\n'
              page_html += '<div class="nav" style="margin-bottom: 20px;">\n'
              page_html += '<a href="../index.html">← Back</a>\n'
              page_html += '</div>\n'
              page_html += '<div class="header">\n'
              page_html += f'<h1>{(page["title"] or "Page")[:50]}</h1>\n'
              page_html += f'<div class="meta"><b>URL:</b> {page["url"]}</div>\n'
              page_html += f'<div class="meta"><b>Status:</b> {page["status_code"]}</div>\n'
              page_html += '</div>\n'
              page_html += '<div style="margin-top: 30px; line-height: 1.8;">\n'
              page_html += (page['content'] or '')[:5000]
              page_html += '\n</div>\n'
              page_html += '</div>\n'
              page_html += '</body>\n'
              page_html += '</html>\n'
              
              with open(page_file, 'w', encoding='utf-8') as f:
                f.write(page_html)
            
            conn.close()
            print(f"  ✅ Done\n")
          
          print("="*60)
          print("✅ Build complete!")
          print("="*60)
          EOF

      - name: 📤 Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: sites-build
          path: sites/
          retention-days: 1

  # ============================================================
  # JOB 3: DEPLOY - Upload to GitHub Pages
  # ============================================================
  deploy:
    name: '🚀 Deploy to Pages'
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/
    
    steps:
      - name: 📂 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 📥 Download build
        uses: actions/download-artifact@v4
        with:
          name: sites-build
          path: sites/

      - name: 🔧 Configure Pages
        uses: actions/configure-pages@v4

      - name: 🏗️ Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site

      - name: 📤 Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './_site'

      - name: 🚀 Deploy to GitHub Pages
        uses: actions/deploy-pages@v4

      - name: 📊 Summary
        run: |
          echo ""
          echo "╔════════════════════════════════════════════════════════╗"
          echo "║          ✅ DEPLOYMENT SUCCESSFUL                      ║"
          echo "╚════════════════════════════════════════════════════════╝"
          echo ""
          echo "📖 Details:"
          echo "  • Domain: ${{ inputs.site_domain }}"
          echo "  • Source: ${{ inputs.source_repo }}"
          echo "  • Run ID: ${{ inputs.run_id }}"
          echo "  • Workflow: ${{ github.run_id }}"
          echo ""
          echo "🌐 Pages URL:"
          echo "  https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
          echo ""
          echo "✨ Your sites are LIVE!"
          echo ""
